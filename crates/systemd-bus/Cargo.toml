[package]
name = "systemd-bus"
version = "0.1.0"
edition = "2024"

[dependencies]
zbus = { workspace = true }
bon = { workspace = true }
futures-lite = { workspace = true }
async-lock = "3.4.2"

[dev-dependencies]
tokio = { version = "1.52.3", features = ["rt", "signal", "macros"]}
rstest = "0.26.1"
fastrand = "2.4.1"
nix = { workspace = true }
rustix = { version = "1.1.4", default-features = false, features = ["process", "fs", "mount", "thread"]}
